{
  "name": "windjammer-production-app",
  "version": "2.0.0",
  "description": "Production coordination app for Windjammer — Inside Stage & Beach Stage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev:server": "node server.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "googleapis": "^134.0.0",
    "jsonwebtoken": "^9.0.2",
    "web-push": "^3.6.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
